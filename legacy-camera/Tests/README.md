# Legacy Tests

The Foundation-only Manifest parser runner lives under `tools/test-legacy-parser` so the same source can be checked on the current host. Device and archived-toolchain tests will be added when that environment is available.

